#pragma once

#include "Pointer.h"
#include "TestRunner.h"

namespace e1
{
    /**
     * Tests for the ByteCountingAllocator type.
     */
    void ByteCountingAllocatorTests(const P<TestRunner>& runner);
}